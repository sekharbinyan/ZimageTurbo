services:
  zimage-turbo:
    build: .
    container_name: zimage-turbo
    # Host network lets the UI reach ComfyUI at 127.0.0.1:8188 on the same GPU server.
    network_mode: host
    environment:
      COMFYUI_URL: ${COMFYUI_URL:-http://127.0.0.1:8188}
    restart: unless-stopped
